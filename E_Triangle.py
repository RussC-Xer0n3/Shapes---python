class E_Triangle(float **kwargs):
	def e_triangle__init__(self, **kwargs):
		self.X == 0
		self.Y == 0
		self.Z == 0
		self.n_x < X
		self.n_y < Y
		self.n_z < Z
		self.p_x > X
		self.p_y > Y
		self.p_z > Z
		for n in (n < X or n > X):
			triangle = (n + (n * 2))
		for n in (n < Y or n > Y):
			triangle = (n + (n * 2))
		for n in (n < Z or n > Z):
			triangle = (n + (n * 2))
	return triangle
