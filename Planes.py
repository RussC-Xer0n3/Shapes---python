class Square(float **kwargs):
	def square__init__(self, **kwargs):
		self.X = 0
		self.Y = 0
		self.Z = 0
		self.n_x < X
		self.n_y < Y
		self.n_z < Z
		self.p_x > X
		self.p_y > Y
		self.p_z > Z
		for n in (n < X or n > X):
			square = pow(n, 2)
		for n in (n < Y or n > Y):
			square = pow(n, 2)
		for n in (n < Z or n > Z):
			square = pow(n, 2)
	return square