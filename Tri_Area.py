''''
Purpose is to represent one triangulation of the surface area

NEEDS MORE WORK SOECIFICALLY WITH DETERMINING TRIANGULATION SIZE (AREA)

''''


import Lon_Lat_Hor
class Tri_Area(float **kwargs):
	def tri_area__init__(self, **kwargs):
		self.lon =  lon_lat_hor.lon() = 0.000
		self.lat = lon_lat_hor.lat() = 0.000
		self.hor = lon_lat_hor.hor() = 0.000
		for n in (lon, lat, hor):
			self.tri_area = (n + (n * 2))
	return tri_area
''''
Purpose is to represent one triangulation of the surface area

NEEDS MORE WORK SOECIFICALLY WITH DETERMINING TRIANGULATION SIZE (AREA)

''''


import Lon_Lat_Hor
class Tri_Area(float **kwargs):
	def tri_area__init__(self, **kwargs):
		self.lon =  lon_lat_hor.lon() = 0.000
		self.lat = lon_lat_hor.lat() = 0.000
		self.hor = lon_lat_hor.hor() = 0.000
		for n in (lon, lat, hor):
			self.tri_area = (n + (n * 2))
	return tri_area
