class Circle(float **kwargs):
	def circle__init__(self, **kwargs):
		self.X == 0
		self.Y == 0
		self.Z == 0
		self.n_x < X
		self.n_y < Y
		self.n_z < Z
		self.p_x > X
		self.p_y > Y
		self.p_z > Z
		self._PI_ = 3.141592653589793
		self.rad
		self.dia
			if rad and dia:
				print('ERROR_RADIUS_AND_DIAMETER_SUPPLIED')
			elif rad:
				for n in (n < X or n > X):
					circle = (_PI_ * pow(rad, 2))
				for n in (n < Y or n > Y):
					circle = (_PI_ * pow(rad, 2))
				for n in (n < Z or n > Z):
					circle = (_PI_ * pow(rad, 2))
			else:
				for n in (n < X or n > X):
					circle = (_PI_ * pow((dia / 2), 2))
				for n in (n < Y or n > Y):
					circle = (_PI_ * pow((dia / 2), 2)
				for n in (n < Z or n > Z):
					circle = (_PI_ * pow((dia / 2), 2)
	return circle
