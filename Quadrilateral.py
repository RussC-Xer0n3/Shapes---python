class Quad(float **kwargs)
	def quad__init__(self, **kwargs):
		self.X == 0
		self.Y == 0
		self.Z == 0
		self.n_x < X
		self.n_y < Y
		self.n_z < Z
		self.p_x > X
		self.p_y > Y
		self.p_z > Z
		for n_x:
			n = n_z or n_y
			quad = (pow(n_x, 2) - pow(n, 3))
		for n_x == n_z:
			quad = (pow(n_x, n_z) - pow(n_y, 3))
		for n_y:
			n = n_z or n_x
			quad = (pow(n_y, 2) - pow(n, 3))
		for n_x == n_y:
			quad = (pow(n_x, n_y) - pow(n_z, 3))
		for n_z:
			n = n_x or n_y
			quad = (pow(n_z, 2) - pow(n, 3))
		for n_z == n_y:
			quad = (pow(n_y, n_z) - pow(n_x, 3))
		for p_x:
			n = p_z or p_y
			quad = (pow(p_x, 2) - pow(n, 3))
		for p_x == p_z:
			quad = (pow(p_x, p_z) - pow(p_y, 3))
		for p_y:
			n = p_z or p_x
			quad = (pow(p_y, 2) - pow(n, 3))
		for p_x == p_y:
			quad = (pow(p_x, p_y) - pow(p_z, 3))
		for p_z:
			n = p_x or p_y
			quad = (pow(p_z, 2) - pow(n, 3))
		for p_z == p_y:
			quad = (pow(p_y, p_z) - pow(p_x, 3))
	return  quad
