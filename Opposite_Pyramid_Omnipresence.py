import lon_lat_hor
class Op_Py_Om(float **kwargs):
	self.lon =  lon_lat_hor.lon()
	self.lat = lon_lat_hor.lat()
	self.hor = lon_lat_hor.hor()
	def op_py_om__init__(self, **kwargs):
		for n in (lon, lat, hor):
			if n == n:
				self.op_py_om = pow((n + (n * 3), 3)
			else:
				print('ERROR_INPUT_NOT_EQUAL')
	return op_py_om
          ''''
          Makes pyramids with 4 rounded faces and rounded edges
          ''''