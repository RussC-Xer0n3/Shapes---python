class Rhombus(float **kwargs):
	def rhombus__init__(self, **kwargs):
		self.X == 0
		self.Y == 0
		self.Z == 0
		self.n_x < X
		self.n_y < Y
		self.n_z < Z
		self.p_x > X
		self.p_y > Y
		self.p_z > Z
		if n_x or p_x or n_y or p_y:
			for n in (n_x or p_x or n_y or p_y):
				rhombus = (pow(n, 2) - pow((n_z and p_z), 3))
		elif n_z or p_z or n_x or p_x:
			for n in (n_z or p_z or n_x or p_x):
				rhombus = (pow(n, 2) - pow((n_y and p_y), 3))
		else:
			for n in (n_y or p_y or n_z or p_z):
				rhombus = (pow(n, 2) - pow((n_x and p_x), 3))
	return rhombus
''''
for n in ((n_x or n_y, or n_z) or ((p_x or p_y, or p_z)):
	(pow(n, 2) - pow(n, 3))

# Think this one is a star
for n in ((n < 0) and (n > 0) and (n < -0) and (n > -0)):
	(pow(n, 2) - pow(n, 3))
''''