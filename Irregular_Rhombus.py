class I_Rhombus(float **kwargs):
	def i_rhombus__init__(self, **kwargs):
		self.X == 0
		self.Y == 0
		self.Z == 0
		self.n_x < X
		self.n_y < Y
		self.n_z < Z
		self.p_x > X
		self.p_y > Y
		self.p_z > Z
		if n_x or p_x or n_y or p_y:
			for n in (n_x or p_x and n_y or p_y):
				i_rhombus = (pow(n, 2) - pow((n_z and or p_z), 3))
		elif n_z or p_z or n_x or p_x:
			for n in (n_z or p_z and n_x or p_x):
				i_rhombus = (pow(n, 2) - pow((n_y and or p_y), 3))
		else:
			for n in (n_y or p_y and n_z or p_z):
				i_rhombus = (pow(n, 2) - pow((n_x and or p_x), 3))
	return i_rhombus
''''
for n in ((n < 0), (n > 0)):
	((n * n) - pow(n, 3))

for n in ((n < 0), (n > 0), (n < -0), (n > -0)):
	((n * n) - pow(n, 3))
''''