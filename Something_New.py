class something_new(float **kwargs):
	def something_new__init__(self, **kwargs):
		self._PI_ = 3.141592653589793
		self.X == 0
		self.Y == 0
		self.Z == 0
		self.n_x < X
		self.n_y < Y
		self.n_z < Z
		self.p_x > X
		self.p_y > Y
		self.p_z > Z
		self.n_rad < 0
		self.p_rad > 0
		self.n_circ < 0
		self.p_circ > 0
		if n_rad or p_rad:
			self.something_new = pow((_PI_ * pow((n_rad or p_rad), ((n > 0) or (n < 0)))), (_PI_ * pow((n_rad or p_rad), ((n > 0) or (n < 0)))))
		elif n_rad and p_rad:
			self.something_new = pow((_PI_ * pow((n_rad and p_rad), ((n > 0) and (n < 0)))), (_PI_ * pow((n_rad and p_rad), ((n > 0) and (n < 0)))))
		else:
			self.something_new = pow((_PI_ * pow((n_rad, p_rad), ((n > 0), (n < 0)))), (_PI_ * pow((n_rad, p_rad), ((n > 0), (n < 0)))))
	return something_new
	
	Torus (mathsisfun.com)
	Torus -- from Wolfram MathWorld
	Vortex Mathematics: Decoding The Fingerprint Of God – Omnia (omniaradiationbalancer.com)
	
	
	
	
	b411dd296761bca757c3c0103a668c1e.jpg (736×961) (pinimg.com)
	
	Walter Russell
