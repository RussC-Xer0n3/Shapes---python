class Cone(float **kwargs):
	Pi = 3.141592653589793 e
	r = 0
	circle = PI * (r ** 2)
	def cone__init__(self, **kwargs):
		self.X == 0
		self.Y == 0
		self.Z == 0
		self.n_x < X
		self.n_y < Y
		self.n_z < Z
		self.p_x > X
		self.p_y > Y
		self.p_z > Z
		n = (X or Y or Z or p_x or p_y or p_z or n_x or n_y or n_z)
		for circle in range (0, n, 1):
		''''
		Where n is the verticle or horizontal value
		''''
			circle += (circle ** -circle)
		''''
		Probably wrong
		''''
return cone