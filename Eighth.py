''''
Written to return the value of one eight if the volume of the sphere.

''''

import E_Sphere
import Lon_Lat_Hor
class Eighth(float **kwargs):
	def eighth__init__(self, **kwargs):
		self.lon =  lon_lat_hor.lon()
		self.lat = lon_lat_hor.lat()
		self.hor = lon_lat_hor.hor()
		for n in (lon, lat, hor):
			if n == n:
				self.eighth = ((n + (n * 3)) - pow(e_sphere.radii(), (n + ( n * 3)))
				# ((n + (n * 3)) - pow(E_Sphere.radii(), 3))
				# ((pow((n), 2), - pow(E_Sphere.rad(), 3))
				# ((pow((n), 2), -pow(E_Sphere.rad(), 3))
				# pow((((n) / 2) * -E_Sphere.rad()), 3)
		else:
			print('ERROR_INPUT_n_NOT_EQUAL')
	return eighth
''''
Written to return the value of one eight if the volume of the sphere.

''''

import E_Sphere
import Lon_Lat_Hor
class Eighth(float **kwargs):
	def eighth__init__(self, **kwargs):
		self.lon =  lon_lat_hor.lon()
		self.lat = lon_lat_hor.lat()
		self.hor = lon_lat_hor.hor()
		for n in (lon, lat, hor):
			if n == n:
				self.eighth = ((n + (n * 3)) - pow(e_sphere.radii(), (n + ( n * 3)))
				# ((n + (n * 3)) - pow(E_Sphere.radii(), 3))
				# ((pow((n), 2), - pow(E_Sphere.rad(), 3))
				# ((pow((n), 2), -pow(E_Sphere.rad(), 3))
				# pow((((n) / 2) * -E_Sphere.rad()), 3)
		else:
			print('ERROR_INPUT_n_NOT_EQUAL')
	return eighth
